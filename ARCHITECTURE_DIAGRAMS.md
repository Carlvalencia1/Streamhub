# 🎨 Diagrama Visual de la Arquitectura

## 📊 Estructura Clean Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                           │
│                                                                 │
│  ┌──────────────────┐      ┌──────────────────────┐           │
│  │                  │      │                      │           │
│  │  LoginScreen     │      │  RegisterScreen      │           │
│  │  ┌────────────┐  │      │  ┌────────────────┐  │           │
│  │  │ Email      │  │      │  │ Username       │  │           │
│  │  │ Password   │  │      │  │ Email          │  │           │
│  │  │ Button     │  │      │  │ Password       │  │           │
│  │  └────────────┘  │      │  │ Confirm Pass   │  │           │
│  └────────┬─────────┘      │  │ Button         │  │           │
│           │                └────────────────────┘  │           │
│           │                        ▲               │           │
│           │                        │               │           │
│           ├────────────────────────┤               │           │
│           │                        │               │           │
│  ┌────────▼──────────────────────────────────┐    │           │
│  │                                           │    │           │
│  │  AuthViewModel                           │    │           │
│  │  ├─ login(email, password)              │    │           │
│  │  ├─ register(username, email, password) │    │           │
│  │  └─ authState: StateFlow<AuthState>     │    │           │
│  │                                          │    │           │
│  └────────┬──────────────────────────────────┘    │           │
│           │                                       │           │
└───────────┼───────────────────────────────────────┘           │
            │
            │ uses
            │
┌───────────▼───────────────────────────────────────────────────┐
│                    DOMAIN LAYER                               │
│                                                               │
│  ┌──────────────────────────────────────────────┐            │
│  │ AuthRepository (Interface)                  │            │
│  │ ├─ suspend login(email, pass): AuthResult   │            │
│  │ └─ suspend register(...): AuthResult        │            │
│  └──────────────────────────────────────────────┘            │
│                      ▲                                        │
│                      │ implements                             │
│                      │                                        │
│  ┌──────────────────────────────────────────────┐            │
│  │ Use Cases                                   │            │
│  │ ├─ LoginUseCase(repository)                │            │
│  │ │  └─ invoke(email, password)             │            │
│  │ └─ RegisterUseCase(repository)             │            │
│  │    └─ invoke(username, email, password)    │            │
│  └──────────────────────────────────────────────┘            │
│                                                               │
│  ┌──────────────────────────────────────────────┐            │
│  │ AuthResult (Sealed Class)                   │            │
│  │ ├─ Success(data: String)                   │            │
│  │ ├─ Error(message: String)                  │            │
│  │ └─ Loading                                 │            │
│  └──────────────────────────────────────────────┘            │
│                                                               │
└───────────────────────────────────────────────────────────────┘
            ▲
            │ implements/uses
            │
┌───────────┴───────────────────────────────────────────────────┐
│                    DATA LAYER                                 │
│                                                               │
│  ┌──────────────────────────────────────────────┐            │
│  │ AuthRepositoryImpl                          │            │
│  │ ├─ authApiService: AuthApiService          │            │
│  │ └─ override login/register()                │            │
│  └──────────────────────────────────────────────┘            │
│                      │                                        │
│                      │ uses                                  │
│                      │                                        │
│  ┌──────────────────┬───────────────────────────┐            │
│  │                  │                           │            │
│  │  AuthApiService  │  AuthInterceptor         │            │
│  │  (Retrofit)      │  (Future)                │            │
│  │  ├─ login()      │  └─ Agregar Bearer token│            │
│  │  └─ register()   │                          │            │
│  │                  │                          │            │
│  └──────────────────┴────────────┬──────────────┘            │
│                                  │                            │
│                                  │ uses                       │
│                                  │                            │
│  ┌──────────────────────────────▼──────────────┐            │
│  │ OkHttpClient                               │            │
│  │ └─ baseUrl: http://localhost:8080          │            │
│  └────────────────────────────────────────────┘            │
│                                                               │
└──────────────────────────────────┬────────────────────────────┘
                                   │
                                   │ HTTP Calls
                                   │
                ┌──────────────────▼────────────────┐
                │   Backend API Server              │
                │   http://localhost:8080           │
                │   ├─ POST /api/users/login        │
                │   └─ POST /api/users/register     │
                └───────────────────────────────────┘
```

---

## 🔄 Flujo de Datos - Login

```
┌─────────────────────────────────────────────────────────────────┐
│                  USER INTERACTION                               │
│                                                                 │
│  Email Input: "user@mail.com"                                 │
│  Password Input: "123456"                                      │
│  Button Click: "Iniciar Sesión"                              │
│                                                                 │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  LoginScreen.onClick()                                          │
│  ├─ Valida campos (no vacíos)                                 │
│  └─ viewModel.login(email, password)                          │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AuthViewModel.login()                                          │
│  ├─ viewModelScope.launch { }                                 │
│  ├─ _authState.emit(isLoading = true)                        │
│  └─ val result = loginUseCase(email, password)               │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  LoginUseCase.invoke()                                          │
│  └─ return authRepository.login(email, password)              │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AuthRepositoryImpl.login()                                      │
│  ├─ try {                                                       │
│  │   val response = authApiService.login(LoginRequest(...))   │
│  │   return AuthResult.Success(response.token)                │
│  │ }                                                            │
│  └─ catch (e) { return AuthResult.Error(e.message) }          │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AuthApiService.login()                                         │
│  └─ POST /api/users/login with LoginRequest                  │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
         ┌──────────────────────────────┐
         │  HTTP POST Request            │
         │  TO: localhost:8080          │
         │  /api/users/login            │
         │  Body: {email, password}     │
         └──────────────────────────────┘
                        │
                        ▼
         ┌──────────────────────────────┐
         │  Backend Response             │
         │  {token: "JWT..."}           │
         │  OR                           │
         │  {error: "Invalid creds"}    │
         └──────────────────────────────┘
                        │
         ┌──────────────┴──────────────┐
         │                             │
         ▼ Success                     ▼ Error
    ┌─────────────┐             ┌──────────────┐
    │ 200 OK      │             │ 400/401      │
    │ {token}     │             │ {error}      │
    └──────┬──────┘             └────┬─────────┘
           │                         │
           ▼                         ▼
    AuthResult                 AuthResult
      .Success(token)            .Error(msg)
           │                         │
           └──────────┬──────────────┘
                      │
                      ▼
         ┌─────────────────────────┐
         │ Retorna a AuthViewModel │
         │ when result { ... }     │
         └──────────────┬──────────┘
                        │
         ┌──────────────┴──────────────┐
         │                             │
         ▼ Success                     ▼ Error
    _authState.emit(              _authState.emit(
      isAuthenticated = true,       error = message,
      token = JWT,                  isLoading = false
      isLoading = false             )
    )                              │
         │                         │
         │                         ▼
         │                  LaunchedEffect
         │                  showSnackbar(error)
         │
         ▼
    LaunchedEffect
    observa isAuthenticated = true
         │
         ▼
    navController.navigate(
      Screen.Home.route,
      popUpTo(Login) { inclusive }
    )
         │
         ▼
    MainScreen detecta
    currentRoute == Home.route
         │
         ▼
    Renderiza Drawer + TopBar
         │
         ▼
    HomeScreen muestra
    con navegación completa
```

---

## 🎨 Interfaz de Usuario - Navigation States

```
┌────────────────────────────────────────────────┐
│  APP STARTUP                                   │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │      LOGIN SCREEN (No Drawer/TopBar)    │ │
│  │                                          │ │
│  │  ┌──────────────────────────────────┐   │ │
│  │  │ Inicia Sesión                   │   │ │
│  │  │                                  │   │ │
│  │  │ [Email        ] @              │   │ │
│  │  │ [Contraseña   ] ●●●●           │   │ │
│  │  │                                  │   │ │
│  │  │  [Iniciar Sesión]              │   │ │
│  │  │                                  │   │ │
│  │  │ ¿No tienes cuenta? Regístrate  │   │ │
│  │  └──────────────────────────────────┘   │ │
│  │                                          │ │
│  └──────────────────────────────────────────┘ │
│                                                │
└────────────────────────────────────────────────┘
              ▲                    ▲
              │                    │
         [Error]              [Clic Registrar]
              │                    │
              ▼                    ▼
    ┌─────────────────┐   ┌──────────────────────┐
    │  SNACKBAR       │   │ REGISTER SCREEN      │
    │  "Invalid creds"│   │ (No Drawer/TopBar)   │
    │                 │   │                      │
    └─────────────────┘   │ ┌────────────────────┤
                          │ │ Crea tu cuenta     │
                          │ │                    │
                          │ │ [Username  ]       │
                          │ │ [Email     ]       │
                          │ │ [Contraseña]       │
                          │ │ [Conf Pass ]       │
                          │ │                    │
                          │ │ [Registrarse]      │
                          │ │                    │
                          │ │ ← (Back)           │
                          │ └────────────────────┤
                          │                      │
                          └──────────────────────┘
                                    │
                            [Clic Registrarse]
                            (o Back)
                                    │
                                    ▼
                        Vuelve a LOGIN SCREEN


┌────────────────────────────────────────────────┐
│  AFTER SUCCESSFUL LOGIN                        │
│                                                │
│  ┌────┬───────────────────────────────────────┐│
│  │ ☰  │  Streamhub              ⋯             ││
│  ├────┼───────────────────────────────────────┤│
│  │    │                                        ││
│  │ In │  Welcome to Home Screen               ││
│  │ Po │                                        ││
│  │ st │  Navigation drawer now visible         ││
│  │    │                                        ││
│  │ Ma │  ┌─────────────────────────────────┐ ││
│  │ sc │  │ Inicio                        ✓ │ ││
│  │ ot │  │ Posts                            │ ││
│  │ as │  │ Mascotas                         │ ││
│  │    │  └─────────────────────────────────┘ ││
│  │    │                                        ││
│  └────┴───────────────────────────────────────┘│
│                                                │
└────────────────────────────────────────────────┘
         ▲
         │
         └─── Click ☰ to open drawer
              Drawer closes after navigation
              Can navigate between screens
```

---

## 🔐 Estado Flow Diagram

```
                    INITIAL STATE
                         │
            ┌────────────┴────────────┐
            │                         │
            ▼                         ▼
      isLoading: false         isAuthenticated: false
      token: null              error: null

            │
            │ User presses "Iniciar Sesión"
            ▼

                 LOADING STATE
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
    isLoading: true         (Making API Call)
    token: null
    isAuthenticated: false
    error: null

        │
        └─────────────────┬──────────────────┐
                          │                  │
                    ┌─────▼────┐      ┌──────▼────┐
                    │ SUCCESS   │      │  FAILURE  │
                    └─────┬────┘      └──────┬────┘
                          │                  │
        ┌─────────────────▼─┐      ┌────────▼────────┐
        │ AUTHENTICATED      │      │  ERROR STATE    │
        │ isLoading: false   │      │ isLoading: false│
        │ token: JWT...      │      │ token: null     │
        │ isAuthenticated: ✓ │      │ error: "Msg"    │
        │ error: null        │      └────────┬────────┘
        └────────────────────┘               │
                    │                        │
                    │                    User reads
                    │                    error & retries
                    │                        │
                    ▼                        └────┐
            Home Screen                          │
            (Drawer enabled)                     │
                                               ▼
                                           INITIAL STATE
                                           (again)
```

---

## 📊 Component Dependency Graph

```
LoginScreen
    │
    └─→ AuthViewModel (Injected by Hilt)
         │
         ├─→ LoginUseCase (Injected)
         │    └─→ AuthRepository (Injected)
         │         └─→ AuthRepositoryImpl (Injected)
         │             └─→ AuthApiService (Injected)
         │                  └─→ Retrofit (@StreamhubRetrofit)
         │
         └─→ RegisterUseCase (Injected)
              └─→ AuthRepository (same as above)


RegisterScreen
    │
    └─→ AuthViewModel (same instance via Hilt)
         └─→ (same dependency tree)


MainActivity
    │
    └─→ MainScreen
         ├─→ NavController (rememberNavController)
         ├─→ DrawerState (rememberDrawerState)
         │
         └─→ AppNavGraph
             ├─→ LoginScreen
             ├─→ RegisterScreen
             ├─→ HomeScreen
             ├─→ PostsScreen
             └─→ PetsScreen
                  │
                  └─→ AppDrawer
                  └─→ AppTopBar
```

---

## 🎯 Hilt Injection Diagram

```
@AndroidEntryPoint
MainActivity
    │
    ├─ requests: NavController
    ├─ requests: DrawerState
    └─ requests: MainScreen()
           │
           └─ requests: AppNavGraph()
                  │
                  ├─ requests: LoginScreen()
                  │    └─ requests: AuthViewModel()
                  │         ├─ LoginUseCase (provided by AuthModule)
                  │         └─ RegisterUseCase (provided by AuthModule)
                  │
                  └─ requests: RegisterScreen()
                       └─ requests: AuthViewModel()
                            (same instance - Singleton)


@Module @InstallIn(SingletonComponent::class)
NetworkModule
    │
    └─ provides: @StreamhubRetrofit Retrofit
            │
            └─ used by: @Module @InstallIn
                        AuthModule
                            │
                            └─ provides: AuthApiService
                                 │
                                 └─ used by: AuthRepositoryImpl
                                      │
                                      └─ implements: AuthRepository
                                           │
                                           └─ used by: Use Cases
                                                │
                                                └─ used by: AuthViewModel
                                                     │
                                                     └─ used by: Screens
```

Este diagrama completo representa toda la arquitectura, flujos y dependencias del sistema.

